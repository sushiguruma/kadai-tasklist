<h1>タスク一覧</h1>

<%= link_to 'タスクの追加', new_task_path %>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to '[削除]', task, method: :delete, data: {confirm: 'タスクを削除してもよろしいですか？'} %>
    <%= link_to task.id, task %>:<%= task.content %>　/状態: <%= task.status %></li>
  <% end %>
</ul>
